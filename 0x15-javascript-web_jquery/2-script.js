// Adds a click event listener to the <div> element with ID 'red_header'
$('div#red_header').click(function() {
    // Updates text color of the <header> element to red
    $('header').css('color', '#FF0000');
});
