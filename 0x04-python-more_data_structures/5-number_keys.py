#!/usr/bin/python3
def number_keys(a_dictionary):
    count_key = 0

    #  loop through each key in the dictionary
    for key in a_dictionary:

        #   Incrementing count by 1 for each key
        count_key += 1

    #  The total number of keys in the dictionary
    return count_key
